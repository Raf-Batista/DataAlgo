// not solved

function anagramsInString(s, t) {
    const result = []; 

    // .split('').sort().join('')
    // .split('').splice(0,2).sort().join('')
    return result;
}

module.exports = anagramsInString